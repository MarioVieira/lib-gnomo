package co.uk.baremedia.gnomo.core
{
	import org.as3.mvcsc.interfaces.IMappingMediator;
	import org.robotlegs.core.IMediatorMap; 
	
	public class MapViews implements IMappingMediator
	{
		public function mapMediators(mediatorMap:IMediatorMap):void
		{
			// GenreListScreen
			//mediatorMap.mapView(ComponentDownloader, MediatorDownloader);
		}
	}
}